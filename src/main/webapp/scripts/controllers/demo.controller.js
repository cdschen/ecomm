'use strict';

angular.module('ecommApp')

.controller('DemoController', ['$scope',
    function($scope) {
        $scope.demo = {};
    }
])

.controller('DemoOperatorController', ['$scope',
    function($scope) {
        $scope.demo = {};
    }
]);
